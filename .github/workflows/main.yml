on:
  workflow_dispatch:
  push:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - uses: lokesh755/my-action/composite@main
        with: 
          action_repository: lokesh755/myaction
          actions_ref: main
